# Laboratorio_de_Progamacion
laboratorio de programacion 2026.
--verificar version de git--
git --version

--configuracion base--
git config --global user.name "Tobias Hernandez"
git config --global user.email "tobiasjosehernandez@gmail.com"

--verificar configuracion--
git config --list

--inicializar repositorio local--
git init

--verificar estado del repo--
git status

--agregar un archivo--
git add nombre_del_archivo

--agregar todos los archivos--
git add .

--realizar el comit--
git commit -m "Mi primer commit"

--ver los comit realizados--
git log

--para salir letra q--

--limpiar consola--
clear

--crear rama principal main
git branch -M main

--
git remote add origin URL
